// Generated by CoffeeScript 1.6.3
(function() {
  var BaseAttribute, BaseNode, BracketupCompiler, Item, ItemGroup, LanguageTitleAttribute, Structure, StructureGroup, TitleAttribute, bracketup, correspondenceCompiler, correspondenceTopLevelFunctionMap, inspect, utils, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  utils = require("./utils.js");

  inspect = utils.inspect;

  bracketup = require("./bracketup.js");

  BaseAttribute = bracketup.BaseAttribute;

  BaseNode = bracketup.BaseNode;

  BracketupCompiler = bracketup.BracketupCompiler;

  Item = (function(_super) {
    __extends(Item, _super);

    function Item(id) {
      this.id = id;
      Item.__super__.constructor.call(this);
    }

    Item.prototype.createInitialDom = function(document) {
      var id, nodeOptions;
      id = this.id;
      if (id.match(/^[0-9]+$/)) {
        id = this.parent.id + id;
      }
      nodeOptions = {
        className: "item",
        attributes: {
          "data-id": id
        }
      };
      return document.createNode("span", nodeOptions);
    };

    return Item;

  })(BaseNode);

  ItemGroup = (function(_super) {
    __extends(ItemGroup, _super);

    function ItemGroup(id) {
      this.id = id;
      ItemGroup.__super__.constructor.call(this);
    }

    ItemGroup.prototype.defaultChildFunction = "item";

    ItemGroup.prototype.classMap = {
      item: Item
    };

    ItemGroup.prototype.childIndent = "  ";

    ItemGroup.prototype.createInitialDom = function(document) {
      var nodeOptions;
      nodeOptions = {
        className: "item-group",
        attributes: {
          "data-group-id": this.id
        }
      };
      return document.createNode("div", nodeOptions);
    };

    return ItemGroup;

  })(BaseNode);

  LanguageTitleAttribute = (function(_super) {
    __extends(LanguageTitleAttribute, _super);

    function LanguageTitleAttribute() {
      LanguageTitleAttribute.__super__.constructor.call(this, "languageTitle");
    }

    return LanguageTitleAttribute;

  })(BaseAttribute);

  Structure = (function(_super) {
    __extends(Structure, _super);

    function Structure(languageCssClass) {
      this.languageCssClass = languageCssClass;
      Structure.__super__.constructor.call(this);
    }

    Structure.prototype.defaultChildFunction = "itemGroup";

    Structure.prototype.classMap = {
      itemGroup: ItemGroup,
      languageTitle: LanguageTitleAttribute
    };

    Structure.prototype.childIndent = "  ";

    Structure.prototype.indentAllChildren = true;

    Structure.prototype.ignoreWhiteSpaceText = true;

    Structure.prototype.createInitialDom = function(document) {
      var className, div, languageTitle;
      className = this.languageCssClass ? "block " + this.languageCssClass + "-block" : "block";
      div = document.createNode("div", {
        className: className
      });
      languageTitle = this.attributes.languageTitle;
      if (languageTitle) {
        document.createNode("div", {
          parent: div,
          className: "language",
          text: languageTitle
        });
      }
      return div;
    };

    return Structure;

  })(BaseNode);

  TitleAttribute = (function(_super) {
    __extends(TitleAttribute, _super);

    function TitleAttribute() {
      TitleAttribute.__super__.constructor.call(this, "title");
    }

    return TitleAttribute;

  })(BaseAttribute);

  StructureGroup = (function(_super) {
    __extends(StructureGroup, _super);

    function StructureGroup() {
      _ref = StructureGroup.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    StructureGroup.prototype.defaultChildFunction = "block";

    StructureGroup.prototype.classMap = {
      block: Structure,
      title: TitleAttribute
    };

    StructureGroup.prototype.childIndent = "  ";

    StructureGroup.prototype.indentAllChildren = true;

    StructureGroup.prototype.ignoreWhiteSpaceText = true;

    StructureGroup.prototype.createInitialDom = function(document) {
      var div, title;
      div = document.createNode("div", {
        className: "translation"
      });
      title = this.attributes.title;
      if (title) {
        document.createNode("div", {
          parent: div,
          className: "description",
          text: title
        });
      }
      return div;
    };

    return StructureGroup;

  })(BaseNode);

  correspondenceTopLevelFunctionMap = {
    translation: StructureGroup,
    b: bracketup.Bold,
    i: bracketup.Italic,
    a: bracketup.Link
  };

  correspondenceCompiler = new BracketupCompiler(correspondenceTopLevelFunctionMap);

  exports.correspondenceCompiler = correspondenceCompiler;

}).call(this);

/*
//@ sourceMappingURL=correspondence-bracketup.map
*/
